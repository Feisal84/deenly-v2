import { Button } from "./ui/button";

export default function AuthButton() {
  // In dieser Version wird keine Authentifizierung verwendet
  return (
    <div className="flex gap-2">
      {/* Hier können später Authentifizierungsbuttons hinzugefügt werden */}
    </div>
  );
}
